# ⚠️ مشكلة الاتصال بـ Supabase

## المشكلة
الموقع مش قادر يتصل بقاعدة البيانات Supabase.

**الخطأ:** `ERR_NAME_NOT_RESOLVED`

**السبب:** الـ URL بتاع Supabase (`kxuvovqvwtwhtxjnnboo.supabase.co`) مش شغال.

---

## الحلول الممكنة

### 1️⃣ تحقق من مشروع Supabase

1. افتح [Supabase Dashboard](https://supabase.com/dashboard)
2. تأكد إن المشروع لسه موجود
3. لو المشروع اتمسح، لازم تعمل مشروع جديد

### 2️⃣ احصل على الـ URL والـ Key الصحيحين

من Supabase Dashboard:

1. اختار المشروع
2. اضغط على **Settings** (⚙️)
3. اختار **API**
4. انسخ:
   - **Project URL** (مثال: `https://xxxxx.supabase.co`)
   - **anon public key**

### 3️⃣ حدّث الملفات

#### ملف `.env`:
```env
VITE_SUPABASE_URL=https://YOUR_PROJECT_URL.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_ANON_KEY_HERE
```

#### ملف `src/lib/supabase.js`:
```javascript
const supabaseUrl = 'https://YOUR_PROJECT_URL.supabase.co';
const supabaseAnonKey = 'YOUR_ANON_KEY_HERE';
```

### 4️⃣ أعد تشغيل التطبيق

```bash
# أوقف السيرفر (Ctrl+C)
# ثم شغله تاني
npm run dev
```

---

## إذا كنت تريد إنشاء مشروع Supabase جديد

### الخطوات:

1. **إنشاء المشروع:**
   - اذهب إلى [Supabase](https://supabase.com)
   - اضغط "New Project"
   - اختار اسم وكلمة مرور للقاعدة

2. **إنشاء الجداول:**
   - افتح SQL Editor
   - نفذ الكود من ملف `supabase-schema.sql`

3. **تحديث الإعدادات:**
   - احصل على URL و Key
   - حدّث ملفات `.env` و `supabase.js`

4. **إنشاء حساب Admin:**
   - سجل حساب جديد من الموقع
   - نفذ SQL من ملف `make-admin.sql`

---

## التحقق من الاتصال

بعد التحديث، جرب:

```bash
# في PowerShell أو CMD
ping YOUR_PROJECT_URL.supabase.co
```

لو الـ ping شغال، يبقى الاتصال تمام ✅

---

## ملاحظات مهمة

- ⚠️ **لا تشارك الـ anon key علناً** (هي موجودة في الكود حالياً)
- 🔒 استخدم Environment Variables في Production
- 📝 احتفظ بنسخة من الـ URL والـ Key في مكان آمن

---

**تاريخ الإنشاء:** 2026-02-01
