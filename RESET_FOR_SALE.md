# 🧹 تنظيف المشروع للبيع

دليل كامل لتنظيف المشروع وجعله جاهز للبيع كمشروع جديد.

---

## 📋 الخطوات المطلوبة:

### 1️⃣ مسح البيانات من Supabase

#### أ) مسح البيانات من الجداول:

1. افتحي **Supabase Dashboard**: https://supabase.com/dashboard
2. اختاري مشروعك
3. روحي على **SQL Editor**
4. انسخي والصقي الكود ده:

```sql
-- مسح كل الإشعارات
DELETE FROM notifications;

-- مسح كل المنتجات
DELETE FROM products;

-- مسح كل المستخدمين
DELETE FROM profiles;
```

5. اضغطي **Run**

#### ب) مسح المستخدمين من Authentication:

1. في Supabase Dashboard، روحي على **Authentication** → **Users**
2. احذفي كل المستخدمين واحد واحد
3. أو استخدمي الكود ده في SQL Editor:

```sql
-- ملاحظة: ده هيشتغل بس لو عندك صلاحيات service_role
-- لو مش شغال، امسحي المستخدمين يدوياً من Dashboard
```

#### ج) مسح الصور من Storage (اختياري):

1. روحي على **Storage** → **products**
2. احذفي كل الصور

---

### 2️⃣ مسح الـ Git History (اختياري)

لو عايزة تمسحي تاريخ الـ commits:

```bash
# احذفي مجلد .git
rm -rf .git

# ابدأي git جديد
git init
git add .
git commit -m "Initial commit - Clean project"
```

---

### 3️⃣ تحديث ملف README

اعملي README جديد للمشتري:

```markdown
# بيكيا - منصة بيع وشراء المنتجات المستعملة

## المميزات
- نظام تسجيل دخول آمن
- لوحة تحكم للإدارة
- نظام تفاوض على الأسعار
- إشعارات فورية
- تصميم عصري واحترافي

## التثبيت
1. `npm install`
2. أنشئ ملف `.env` وأضف بيانات Supabase
3. `npm run dev`

## البناء للإنتاج
`npm run build`
```

---

### 4️⃣ إزالة البيانات الشخصية

#### من الكود:
- ✅ الكود نظيف بالفعل (يستخدم environment variables)

#### من .env:
- ⚠️ **لا ترفع ملف .env على GitHub**
- اعملي ملف `.env.example` بدون القيم الحقيقية:

```env
VITE_SUPABASE_URL=your-supabase-url
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key
```

---

### 5️⃣ إنشاء Supabase Project جديد (للمشتري)

المشتري هيحتاج يعمل:

1. **إنشاء مشروع جديد** على Supabase
2. **تشغيل الـ SQL Schema**:
   - افتح `supabase-schema.sql`
   - نفذه في SQL Editor
3. **تشغيل الـ Migrations**:
   - نفذ كل ملفات `migration-*.sql` بالترتيب
4. **تحديث .env** بالبيانات الجديدة

---

### 6️⃣ حذف الملفات غير الضرورية

احذفي الملفات دي قبل البيع:

```bash
# ملفات التوثيق
rm CHANGES_SUMMARY.md
rm COLOR_PALETTE.md
rm DEPLOYMENT_GUIDE.md
rm DESIGN_*.md
rm IMPROVEMENTS.md
rm MOBILE_RESPONSIVE.md
rm NEGOTIATION_SYSTEM.md
rm NOTIFICATIONS_*.md
rm PREMIUM_DESIGN_2026.md
rm PRICING_SYSTEM.md
rm QUICK_*.md
rm README_DESIGN.md
rm SAFE_DEPLOY_STEPS.md
rm SELLER_REJECTION_FEATURE.md
rm SUPABASE_SETUP.md
rm TROUBLESHOOTING.md
rm UNIQUE_FEATURES.md

# SQL Scripts الشخصية
rm make-admin.sql
rm update-profile.sql
rm reset-database.sql

# ملفات الاختبار (اختياري)
rm -rf tests/
rm playwright.config.js
rm -rf playwright-report/
rm -rf test-results/

# هذا الملف نفسه
rm RESET_FOR_SALE.md
```

---

### 7️⃣ تنظيف node_modules و dist

```bash
rm -rf node_modules
rm -rf dist
rm -rf .vite
```

المشتري هيعمل `npm install` من جديد.

---

## ✅ Checklist النهائي

قبل ما تبيعي المشروع، تأكدي من:

- [ ] مسح كل البيانات من Supabase
- [ ] مسح كل المستخدمين من Authentication
- [ ] مسح الصور من Storage
- [ ] حذف ملفات التوثيق غير الضرورية
- [ ] تحديث README للمشتري
- [ ] التأكد من عدم وجود بيانات شخصية في الكود
- [ ] عمل `.env.example` بدون القيم الحقيقية
- [ ] اختبار المشروع بعد التنظيف

---

## 📦 ما يجب تسليمه للمشتري

1. **الكود المصدري** (بدون node_modules)
2. **ملف README** بتعليمات التثبيت
3. **ملف supabase-schema.sql** لإنشاء قاعدة البيانات
4. **ملفات migration-*.sql** للتحديثات
5. **ملف .env.example** كمثال

---

## 💰 نصائح للبيع

1. **اعملي Demo Video** يوضح المميزات
2. **اكتبي Documentation** واضح للمشتري
3. **حددي السعر** بناءً على:
   - عدد المميزات
   - جودة الكود
   - التصميم
   - الدعم اللي هتقدميه

---

## 🎯 بعد البيع

لو المشتري محتاج مساعدة:
- ممكن تقدمي دعم فني لمدة محددة
- ممكن تعملي تدريب على استخدام المشروع
- ممكن تساعديه في الـ deployment

---

**بالتوفيق في البيع! 🎉**
