# 🔐 بيانات حساب الأدمن

## معلومات تسجيل الدخول:

- **Username**: `hanoon`
- **Password**: `753123`
- **Role**: `admin`

---

## 📋 للتأكد من صلاحيات الأدمن:

### في Supabase SQL Editor:

```sql
-- التحقق من الحساب
SELECT id, username, email, role, location, phone 
FROM profiles 
WHERE username = 'hanoon';

-- تحديث الـ role لـ admin (إذا لزم الأمر)
UPDATE profiles 
SET role = 'admin' 
WHERE username = 'hanoon';

-- تحديث البيانات الكاملة
UPDATE profiles 
SET 
  role = 'admin',
  location = 'awsim',
  phone = '01002284046'
WHERE username = 'hanoon';
```

---

## 🎯 الوصول للوحة الإدارة:

بعد تسجيل الدخول:
- **URL**: `/admin`
- **في الـ Navbar**: "لوحة الإدارة"

---

## ⚠️ ملاحظات مهمة:

1. لازم تسجل خروج ودخول بعد تغيير الـ role
2. امسح الـ cache لو مش شغال: `localStorage.clear()`
3. جرب في Private Window لو لسه مش شغال

---

## 🔧 استكشاف الأخطاء:

### لو "لوحة الإدارة" مش ظاهرة:

1. تأكد إن الـ role = 'admin' في قاعدة البيانات
2. سجل خروج ودخول تاني
3. امسح الـ localStorage:
   ```javascript
   localStorage.clear();
   location.reload();
   ```

### لو الصفحة بترجعك للـ Home:

معناها الـ role مش admin. نفذ الـ SQL فوق وسجل دخول تاني.

---

**تاريخ التحديث**: 2026-01-31
