# إعداد حذف المستخدمين بالكامل

## المشكلة
عند حذف مستخدم من التطبيق، يتم حذفه من جدول `profiles` فقط، لكنه يبقى في `auth.users`، مما يسمح له بتسجيل الدخول مرة أخرى.

## الحل
تشغيل Database Function في Supabase لحذف المستخدم من كل الجداول بما فيها `auth.users`.

## خطوات التفعيل

### 1. افتح Supabase Dashboard
- اذهب إلى مشروعك في Supabase
- اضغط على "SQL Editor" من القائمة الجانبية

### 2. شغل الـ Migration
- انسخ محتوى ملف `migration-delete-user-function.sql`
- الصقه في SQL Editor
- اضغط "Run" أو "Execute"

### 3. تأكد من نجاح العملية
يجب أن تظهر رسالة "Success" بدون أخطاء.

## كيف يعمل

بعد تشغيل الـ migration، عند حذف مستخدم من صفحة "إدارة المستخدمين":

1. يتم حذف إشعاراته
2. يتم حذف منتجاته
3. يتم حذفه من `profiles`
4. يتم حذفه من `auth.users` (نهائياً)

## ملاحظات مهمة

- ⚠️ الحذف نهائي ولا يمكن التراجع عنه
- ✅ الأدمن لا يستطيع حذف نفسه (محمي في الكود)
- 🔒 الدالة محمية بـ SECURITY DEFINER
- 📝 يتم حذف كل البيانات المرتبطة بالمستخدم

## إذا لم تشغل الـ Migration

إذا لم تقم بتشغيل الـ migration، سيعمل التطبيق بشكل عادي لكن:
- سيتم حذف المستخدم من `profiles` فقط
- سيظهر تحذير للأدمن بضرورة تشغيل الـ migration
- المستخدم لن يستطيع تسجيل الدخول (لأن profile محذوف)
- لكن حسابه سيبقى في `auth.users`

## التحقق من التفعيل

لمعرفة إذا كانت الدالة مفعلة:

```sql
SELECT routine_name 
FROM information_schema.routines 
WHERE routine_name = 'delete_user_completely';
```

إذا ظهرت نتيجة، معناها الدالة مفعلة ✅
